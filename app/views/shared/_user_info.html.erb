<div class="user_info">
  <a href="<%= user_path(current_user) %>">
    <span class="user_name">
      <%= current_user.name %>
    </span>
    <span class="bookmarks">
      <%= pluralize(current_user.bookmarks.count, "bookmark") %>
    </span>
  </a>
</div>
